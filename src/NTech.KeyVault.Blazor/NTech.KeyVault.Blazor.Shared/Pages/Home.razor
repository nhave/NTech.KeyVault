@page "/"
@inject IUserContext UserContext

<PageTitle>Home</PageTitle>

@* <p>@_user?.FullName</p> *@

@code {
    private ClientUser? _user;

    protected override async Task OnInitializedAsync()
    {
        _user = await UserContext.GetCurrentUserAsync();
    }
}
